# Copy to runner.secret.env (gitignored) and fill.
# Fine-grained org PAT with Organization > Self-hosted runners = Read and write.
# JIT-only: the runner never exposes this to a workflow. Never commit.
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxx
